
class Lightbox {
    constructor(emplacement, images) {
        this.container = document.getElementsByClassName(emplacement);
        this.images = images;
        this.currentIndex = 0;
      }

      nextImage() {

        console.log('test next image !')

      }
      
}

